# 파인튜닝 도입 판단: 2026-09-08

현재 결정: 학습 보류. 도입 효과 없음으로 판정한 것이 아니라 비교 근거가 부족하다.

관찰: rag-local.json과 rag-local-v2.json의 세 생성 사례는 모두 필수 clarification 누락으로 계약 검사에 실패했다. 근거 없는 숙박비와 상충 자료의 임의 선택도 관찰했다. v2는 v1 실패를 보고 수정했으므로 이 질문들은 개발 자료다.

다음 순서: 서비스 계약 유지 → 독립 평가 자료 준비 → 제약 생성·대체 모델 비교 → 반복되는 행동 실패와 충분한 검토 데이터가 남으면 SFT 검토.

고정할 평가 항목: 모든 요청을 분모로 한 완결·계약 통과, 주장별 근거 지지, 답변 가능/불가능 각각의 처리, 권한·승인 회귀, 지연과 비용. 필수 업무 경계를 침해하거나 의미 정확성이 악화된 후보는 형식 성공률만으로 채택하지 않는다. 수치 채택 문턱과 표본 크기는 실제 평가 집합 설계 전에 정한다.

이번 실행: 공개 교육용 9행의 구조·계열·동일 입력 분할 검사. train=4, dev=2, test=3, family=5. 학습·학습 후 추론·향상률·손익분기 실측은 미실행이다. 공개된 test 행을 독립 비공개 평가로 사용하지 않는다.
