# 검증 기록

## 2026-09-08: 1부와 최초 사이트 통합

환경: Python 3.14.6, Node.js v22.23.0. Python 계산 예제는 표준 라이브러리만 사용한다. 사이트 의존성은 저장소의 기존 package-lock.json을 사용했다.

### 실행과 결과

| 검증 | 실제 결과 | 증거의 범위 |
|---|---|---|
| `python3 books/ai-engineering/examples/sampling.py` | 본문에 적은 세 줄 출력과 일치 | 교육용 로짓의 확률 계산 |
| `python3 -m unittest discover -s books/ai-engineering/examples -p 'test_*.py' -v` | 5개 테스트 통과 | 확률 합·순서, 평행 이동 불변성, 균등 분포, 온도 변화, 잘못된 입력 |
| 본문의 Python 코드 블록 추출·실행 | 파일 구현과 세 온도에서 결과 일치 | 코드 전재의 불일치 검사 |
| `npm run build` | 종료 코드 0, 검색 색인 138페이지 | 현재 사이트 정적 빌드·Pagefind 생성 |
| 생성 HTML의 책 내부 링크와 절 앵커 | 대상 존재 | 새 책의 목차·장 링크·본문 앵커 |
| 생성 HTML의 canonical | 표지와 2개 장의 예상 URL과 일치 | 새 책 URL |
| 두 사이트맵의 새 책 URL 비교 | 동일한 3개 URL | 표지와 2개 장의 사이트맵 반영 |
| 2장 수식 렌더링 마크업 | KaTeX 요소 8개 확인 | 수식 처리 성공, 시각 품질은 별도 |

빌드에는 기존 리다이렉트와 검색 확인용 HTML의 `<html>` 요소 관련 Pagefind 경고가 있었다. 경고 목록은 새 책 경로를 가리키지 않았다. 한국어 어간 처리 미지원 안내도 있었으며 검색 색인은 생성됐다. 실제 검색 질의와 브라우저의 반응형 배치·클릭 동작은 아직 검증하지 않았다.

### 기술 검토

1장은 가상 조직·요구사항·상태 계약이 저자의 교육용 설계임을 밝혔다. 모델 출력과 서버 권한을 구분하고, 정책의 최신 수정 시각과 시행일을 구별했다. 평가 사례 20개는 대표성 있는 벤치마크나 실행 완료한 테스트라고 표현하지 않았다.

2장은 학습과 추론, 매개변수와 요청 문맥, 모델과 호스팅 환경을 구분했다. 장문 문맥 연구의 결과는 해당 모델·과제에 한정했으며 오늘날 모든 모델의 실패로 일반화하지 않았다. 온도 0을 양의 온도 수식에 대입하지 않았고, 샘플링과 사실 정확성을 구분했다. 상용 모델의 실제 성능·가격·순위를 지어내지 않았다.

1–2장의 참조 자료는 해당 주장 가까이에 링크했다. 생성 설정 문서는 main 브랜치 자료이며 특정 설치 버전의 동작을 검증한 것처럼 표시하지 않았다. 실제 패키지 예제는 후속 장에서 버전을 고정하고 검증해야 한다.

### 미완료

- 1장 질문 20개에 대한 구체적 문서·티켓 픽스처와 자동 검증.
- 2장 모델 비교의 실제 API 실행과 선택 기록.
- 3–18장과 부록의 본문·예제·검증.
- 본문 전체의 출처·연결·용어·문체 최종 퇴고.
- 책 전체의 브라우저 검토와 실제 검색 검증.

현재 1장 약 10,165자, 2장 약 11,073자다. 공백과 frontmatter 등을 포함한 파일 길이이며 책 전체 분량의 완료 증거가 아니다. 책 메타데이터는 `status: writing`을 유지한다. 원격 게시·커밋·배포는 수행하지 않았다.


## 2026-09-08: 3장 API 경계

환경: Python 3.14.6, openai 3.8.0, httpx2 2.12.0. 예제 전용 `.venv`에 설치했다. 직접 의존성은 `examples/requirements.txt`, 설치 환경 전체 목록은 `examples/requirements-lock.txt`에 기록했다.

- 실제 SDK와 `httpx2.MockTransport`를 사용한 14개 테스트, 기존 계산 테스트 5개를 합쳐 19개 통과.
- 정상 텍스트·사용량, 미완료, 거절, 출력 없음, 도구 요청, HTTP 오류, 연결 실패, 시간 초과, 스트림 완료·중단·읽기 오류, 사용량 누락, 잘못된 JSON을 검증했다.
- 각 모의 호출의 전송 횟수가 1인지 확인했다. `max_retries=0` 기준이며 재시도 정책의 운영 효과를 검증한 것은 아니다.
- API 환경 변수를 제거한 별도 프로세스에서 CLI가 종료 코드 2를 반환하고 stdout을 비우는 것을 확인했다.
- `npm run build` 종료 코드 0, 검색 색인 139페이지.
- 표지와 3개 장의 내부 링크·절 앵커 존재를 검사했다. 두 사이트맵에 동일한 4개 책 URL이 있다.
- 3장 파일은 공백·frontmatter 포함 11,822자다.

기술 검토에서는 생성 성공과 업무상 성공, 거절과 권한 제한, 미상 사용량과 0, 읽기 시간 초과와 전체 실행 마감, 부분 텍스트와 최종 완료를 구분했다. `store=False`를 전체 데이터 보관 부재의 보장으로 설명하지 않았다. SDK 3.8.0의 생성자·로컬 소스로 HTTP 클라이언트 타입과 기본 재시도 횟수를 확인했다.

외부 API 키와 모델 설정은 없었다. 실제 모델 추론, 계정의 모델 접근, 한국어 답변 품질, 실제 지연·비용은 미검증이다. 모의 통과를 모델 품질 검증으로 간주하지 않는다. 전체 요청 마감 시간, 출력 크기 제한, 입력 예산, 실시간 화면 표시, 재시도 운영 정책은 이 CLI의 구현 범위에 없으며 후속 장에서 확장한다.

본문의 코드 블록 중 호출과 데이터 클래스는 전체 파일의 발췌다. 독립 실행은 `model_client.py`를 사용한다. 전체 원고의 브라우저 시각 검토와 검색 질의 실행은 여전히 남아 있다.


## 2026-09-08: 4장 문맥과 비용

- 4장 본문 12,278자(공백·frontmatter 포함), 문맥 구성기·비용 함수·로컬 시연 추가.
- 전체 테스트 32개 통과. 4장 추가 13개는 권한 회수, 조직·사용자·세션 격리, 계수 이전 권한 필터, 예산 경계, 초과 질문 거부, 큰 블록 건너뛰기, 식별자·계수값 검증, JSON 데이터 경계, 계수/생성 입력 동일성, 가상 비용과 미상·오류 사용량을 다룬다.
- 시연에서 선택 자료는 edu-v2 하나, 예산 제외 자료는 long-history 하나이며 권한 밖 자료는 둘 다에 나타나지 않는다. 문자 계수 결과 99를 모델 토큰 수로 간주하지 않는다.
- 가상 단가 계산 결과 0.003 통화 단위를 확인했다. 실제 가격·청구액이 아니다.
- `npm run build` 종료 코드 0, Pagefind 140페이지. 표지와 4개 장의 링크·앵커·양쪽 사이트맵의 5개 URL 일치를 검사했다.

OpenAI 공식 입력 계수, 프롬프트 캐시, 대화 상태 문서를 확인했고 SDK 3.8.0의 계수 메서드도 검사했다. 실제 API 계수는 미실행이다. 테스트는 모의 HTTP로 계수·생성의 최종 입력과 지시문 일치를 확인했다.

한계: 블록 우선순위는 교육용 정수이며 검색 순위가 아니다. 자동 요약, 응답 캐시, 문서 시행일 선택, 동시 요청의 금액 예약은 이번 모듈에 없다. 현재 문맥 창의 실제 모델별 값은 실측하지 않았으며 호출자가 공식 모델 계약에 맞게 지정해야 한다. 계수 후보별 외부 호출은 운영 지연 측면의 최적화가 남아 있다.


## 2026-09-08: 5장 구조화 출력

본문 12,346자(공백·frontmatter 포함)를 추가했다. Pydantic 2.13.5를 직접 의존성으로 명시했다. 설치 환경의 해당 버전은 기존 lock에 이미 포함돼 있다.

- 전체 45개 테스트 통과. 신규 13개는 정상·비응답 상태, 추가·누락·잘못된 타입, 상태 모순, 출처·구절 조작, 중복 인용·JSON 키, 크기·JSON 오류, 스키마 폐쇄성과 필수 필드, 의미 검증의 한계, 계수/생성 스키마 일치, 오류 원문 제거, 거절 분리를 확인했다.
- `answer_demo.py`: 정상 계약 통과, 출처 조작 거부, 의미가 틀려도 실제 인용을 포함하면 계약 검사는 통과하는 한계를 재현했다.
- `npm run build` 종료 코드 0, Pagefind 141페이지.
- `python3 books/ai-engineering/verify_site.py`: 원고 5장과 표지, 내부 링크·앵커·canonical·두 사이트맵의 일치 확인.

생성 계약에 넣은 스키마를 입력 계수에도 동일하게 전달하도록 3–4장 모듈을 확장했고 기존 테스트도 함께 통과했다. 거절·미완료는 구조 파싱 전에 걸러 낸다. 계약 오류 때 원본 출력이 상위 결과에 남지 않는지 검사했다.

한계: 문자열 인용 검증은 주장 지지 관계를 검증하지 않는다. 모든 출처의 최신성·사실성도 증명하지 않는다. 실제 모델의 Structured Outputs 지원과 의미 품질, P0/P1/P2 프롬프트 비교는 외부 실행 전이다. 원문 크기 제한은 응답을 받은 뒤 파싱 전의 제한이며 네트워크 수신 상한이 아니다. 자동 재생성과 상태별 화면·서비스 응답 변환은 후속 작업이다.


## 2026-09-08: 6장 공개 모델과 실제 로컬 추론

본문 12,714자(공백·frontmatter 포함). Qwen/Qwen3-0.6B 리비전 c1899de289a04d12100db370d81485cdf75e47ca를 다운로드해 실제 실행했다. Python 3.14.6, Transformers 5.16.1, torch 2.14.0, safetensors 0.8.0, huggingface-hub 1.30.0. macOS 26.6.2 arm64, CPU 4스레드, float32. 전체 설치 목록은 examples/requirements-local-lock.txt다.

- 최초 다운로드 포함 로드 약 133.10초, 생성 호출 약 1.36초. 원문 기록 experiments/local-first-download.json. 첫 질문의 답변은 문서에 존재하는 교육비 정보를 찾지 못했다.
- 캐시 기반 재실행: 모델 한 번 로드 약 3.53초, 4개 사례 × 2회 추론. experiments/local-cpu.json에 모든 입력·기대 기준·출력·개별 시간·환경을 저장했다. 모든 호출은 EOS로 완료됐지만 L01은 답변 누락, L02는 내용 추출 성공 및 엄격한 형식 실패였다. L03/L04는 유보 기준을 충족했다. 자동 품질 점수가 아니라 원문 대조 판정이다.
- 실제 모델 객체의 파라미터 수 596,049,920, 파라미터 텐서 합계 2,384,199,680바이트. 프로세스 최대 메모리로 해석하지 않는다.
- 별도 실제 추론에서 출력 한도 1토큰은 incomplete·빈 text·output_limit으로 반환됐다. 긴 입력은 생성 전 예산 초과 ValueError를 냈다. 두 조건을 assert로 검사했고 종료 코드 0, 결과는 experiments/local-boundaries.json이다.
- 의존성 추가 후 기존 45개 테스트 모두 통과했다. 이 테스트 묶음은 로컬 가중치를 내려받지 않는다.
- npm run build 종료 코드 0, Pagefind 142페이지. verify_site.py는 6개 장과 표지의 HTML·내부 링크·앵커·canonical·두 사이트맵 일치를 확인했다.

추론은 비사고 모드·샘플링 비활성·새 출력 상한 128토큰이다. 별도 워밍업·동시 요청은 없고 시간은 토큰화·생성·디코딩을 포함한다. 네 사례의 반복을 독립적인 여덟 업무 표본으로 해석하지 않았다. 호스팅 API 품질·비용, MPS, 양자화, 전체 서비스 성능, 구조화 출력 지원은 이 기록으로 검증되지 않는다.

이전 턴의 프롬프트 문장 답변은 원고 상태를 진전시키지 않은 것으로 분류했다. 이번 턴은 기존 실행 핸들 28413의 완료를 확인하고 원문 결과를 보존한 뒤, 새 평가·원고·실패 경로 검증을 수행해 실제 진전이 있었다. 전체 목표는 미완료다.


## 2026-09-08: 7장 임베딩·전수 검색

본문 12,456자(공백·frontmatter 포함). 실제 E5 어댑터, 코사인 전수 검색, 실제 검색·경계 검증 스크립트, 수동 2차원 심화 계산 예제를 추가했다. 모델은 intfloat/multilingual-e5-small, 리비전 614241f622f53c4eeff9890bdc4f31cfecc418b3이다. 6장의 의존성 환경을 그대로 사용했다.

- 기존 45개와 신규 6개 unittest를 합쳐 51개 통과. 기하 관계·극단 크기·영벡터/NaN/무한대·차원, 권한 회수/조직 격리/점수 이전 필터, 동점·k, 공간 혼합·중복 청크를 검사했다. 검색 권한 검사는 4장의 Principal을 사용한다.
- 실제 CPU 임베딩: 가상 문서 5개, 질문 5개. 384차원, float32, 4스레드. 다운로드 포함 로드 41.218초, 문서 임베딩 0.0357초. 입력과 top3 원문·점수·시간은 experiments/embedding-cpu.json에 보존했다.
- E01–E04는 지정 근거가 모두 1위. E05는 근거가 없는 질문이며 국내 교통비 문서가 0.8455로 1위였다. E02의 올바른 휴가 문서 점수 0.8383보다 높아 단순 절대 임계값의 한계를 설명했다. 네 양성 사례의 Recall@1을 전체 서비스 정확도로 일반화하지 않았다.
- 실제 embedding_checks.py 종료 코드 0: 차원 384, L2 노름 1.0, 단독/배치 최대 좌표 차이 8.9980643e-08(<1e-5). 빈 입력·역할 오류·512토큰 초과를 거부했다. 결과는 experiments/embedding-checks.json이다.
- vector_applications.py 실행: 최근접 분류 계정, 추천 순서 설정 안내/수리 안내, 이상 후보 점수 1.7071. 실제 텍스트 임베딩 결과가 아닌 수동 숫자 예제다.
- npm run build 종료 코드 0, Pagefind 143페이지. verify_site.py가 7장과 표지의 경로·링크·앵커·canonical·양쪽 사이트맵을 확인했다.

한계: 짧은 다섯 문서는 주제별로 뚜렷한 교육용 집합이다. 저장소 규모·동시 요청·근사 검색·시행일·문서 갱신은 이 검증에 포함되지 않는다. 검색은 상위 k개를 반환할 뿐 근거 충분성을 판정하지 않으며 실제 생성 연결은 후속 RAG 장에서 구현한다. space 메타데이터는 코드가 만든 계약 식별자이지 위조를 방지하는 증명이 아니다.

이전 goal 턴은 6장 원고·실제 추론·검증 기록을 추가한 progress로 분류한다. 이번 턴도 원고와 구현·실측을 추가한 progress다. 전체 완성 목표는 유지한다.


## 2026-09-08: 8장 문서 저장소·생명주기

본문 11,460자(공백·frontmatter 포함). UTF-8 파일 입력, NFC/줄바꿈 정규화, 토큰 예산 분할, SQLite 스냅샷 교체·최신 ACL·시행일 검색·삭제 기록을 구현했다. Python 3.14.6, SQLite 3.53.2, 임베딩은 기존 E5 환경이다.

- 총 60개 unittest 통과. 신규 9개는 UTF-8 BOM/오류/크기, 문자 구간·정규화·예산, 시행일과 지속 저장, ACL 회수와 주체 교집합, 다른 조직/사용자, 중복·오래된 이벤트·삭제 후 복원, 구간/공간 오류, 임베딩 실패, DB 삽입 오류 롤백, 벡터 생성 중 별도 연결의 최신 갱신을 검증했다.
- DB 내부 실패는 BEFORE INSERT 트리거의 RAISE(ABORT)로 주입하고 sqlite3.IntegrityError와 기존 원문 유지·트랜잭션 종료를 확인했다. 모델 실패만으로 원자성을 주장하지 않았다.
- 실제 E5 + 임시 SQLite + 텍스트 픽스처 시연 store_demo.py 종료 코드 0. 8월 31일 구규정 20만 원, 9월 8일 신규정 30만 원 반환. 재오픈 결과 동일, 같은 Principal로 권한 회수 후 결과 없음, 삭제 후 이전 순번 거부. experiments/store-lifecycle.json에 기록했다.
- npm run build 종료 코드 0, Pagefind 144페이지. verify_site.py는 8장과 표지의 HTML·링크·앵커·canonical·두 사이트맵을 확인했다.

한계: JSON 벡터를 읽어 Python 전수 검색한다. ANN·대규모 지연·부하·분산 변경 복제는 미검증이다. 파서는 UTF-8 텍스트만 지원한다. 청커는 문단 경계를 우선하지만 문자 분할을 허용하며 의미 보존의 최적성을 보장하지 않는다. DB 조회 이후 발생하는 권한 회수는 상위 요청 경계의 재검사가 필요하다. SQL 행 삭제는 디스크·백업의 물리적 삭제를 증명하지 않는다. 새 공간으로의 온라인 이중 색인 전환은 설계 설명이며 자동 구현은 아니다.

이전 goal 턴은 7장 원고·실측·검증을 추가한 progress였다. 이번 턴도 원고와 실제 지속 저장·실패 경로 증거를 추가한 progress다. 전체 목표는 미완료로 유지한다.


## 2026-09-08: 9장 RAG 통합

본문 10,447자(공백·frontmatter 포함). Store.resolve, RagResult/respond, 로컬 생성 백엔드, 실제 통합 시연을 추가했다. LocalModel.prepare를 추출해 계수와 생성이 같은 템플릿 입력을 사용하게 했다.

- 총 71개 unittest 통과. 신규 11개는 정상 출처·리비전 연결, 최초 근거 재검사, 빈 후보의 생성 생략, 예산 부족 구분, 계수 중 권한 회수, 생성 중 권한 회수/문서 교체 후 출력 제거, 예산 제외 출처 거부, 의미 오류의 검증 한계, 비응답·생성 오류, 단계 오류·잘못된 k를 확인했다.
- 실제 E5/SQLite/Qwen 통합: v1과 v2 각각 세 생성 호출 및 권한 회수 사례 한 개. 원본 실험은 experiments/rag-local.json 및 rag-local-v2.json. 모든 생성 호출에서 모델 입력 토큰 수와 백엔드 계수값 일치를 assert로 확인했다.
- v1 입력 574/577/598토큰, 출력 117/100/75토큰. v2 입력 784/787/808토큰, 출력 80/80/51토큰. v2는 코드 블록을 제거했지만 양쪽 모두 clarification 누락으로 invalid_structure였다. 모델 출력을 바꾸거나 파서를 완화하지 않았다.
- R01 v1은 사실 내용이 맞지만 형식 실패, v2는 승인 단계 누락. R02 v1은 교육비 30만 원을 숙박비로 오용, v2는 근거 없는 150,000원 생성. R03은 양쪽 모두 해결되지 않은 문서 충돌에서 50만 원을 선택했다. R04는 모델 호출 없이 insufficient_evidence.
- 모델·라이브러리·장치 조건은 6–8장과 동일하다. v1/v2는 개발용 단일 관찰이며 프롬프트 품질·지연의 통계적 우열을 증명하지 않는다.
- npm run build 종료 코드 0, Pagefind 145페이지. verify_site.py가 9장과 표지의 경로·링크·앵커·canonical·두 사이트맵을 확인했다.

한계: 로컬 백엔드는 제약 디코딩을 제공하지 않는다. 구조/구절 검사는 주장 지지·문서 충돌을 자동 증명하지 않는다. 정상 경로 테스트는 모의 생성기로 실행했다. 실제 모델의 채택 기준은 미통과이며 전체 완성 전에 대안과 평가를 계속해야 한다. 권한 검사는 시점별 재검사이며 최종 검사 이후의 경쟁이나 이미 전송한 자료 회수까지 보장하지 않는다. 현재 계수는 로컬이며 외부 계수 API로 교체하면 별도 전송 경계를 검토해야 한다.

이전 goal 턴은 8장 집필·실제 저장·검증을 추가한 progress였고, 이번 턴도 통합 코드·실측·본문을 추가한 progress다. 전체 목표를 완료로 표시하지 않는다.


## 2026-09-08: 10장 검색 개선 비교

본문 7,775자(공백·frontmatter 포함). FTS5 키워드 검색과 RRF, 실제 E5 비교 스크립트를 추가했다. 최초 테스트는 식별자 질의 한 건에서 실패했다. 숫자·한글 경계를 질의와 검색용 문서에 동일하게 분리한 뒤 전체 75개 테스트가 통과했다. 신규 4개는 식별자/무결과, 질의 문법 문자 처리, RRF 산술/빈 목록/동점, 잘못된 인자·중복 ID를 다룬다.

실측 자료는 가상 문서 7개, 질의 6개다. 원본 experiments/hybrid-unsegmented.json, 수정 후 hybrid-baseline.json. 수정 전 H03/H04의 키워드 결과가 없었고 수정 후 올바른 티켓이 1위였다. 의미 검색은 양성 5개 모두 정답 1위, RRF도 동일해 추가 이득은 관찰되지 않았다. 음성 H06은 세 방법 모두 답이 없는 교통비 자료를 반환했다.

npm run build 종료 코드 0, Pagefind 146페이지. verify_site.py가 10개 장과 표지의 링크·앵커·canonical·두 사이트맵을 검증했다. 사이트 전체 브라우저 시각 검증은 아직 남아 있다.

한계: 키워드 메모리 색인은 질의마다 생성된다. 각 시간 범위가 달라 직접 처리량 비교로 쓰지 않는다. 모든 실측 자료는 같은 주체가 읽을 수 있는 가상 자료이며 신규 키워드 색인의 지속 갱신·권한 통합은 미구현이다. 재순위화 모델·프레임워크·관리형 검색·청크 크기 최적화 실험은 실행하지 않았다. 9장의 생성 오류는 이 비교로 해결되지 않았다.

이전 goal 턴은 9장 통합·실측·집필의 progress다. 이번 턴도 비교 구현·실측·실패 수정·본문 작성으로 progress를 만들었다. 전체 목표는 미완료다.


## 2026-09-08: 11장 티켓 도구와 MCP

본문 8,918자(공백·frontmatter 포함). 가상 티켓 클래스, 실제 MCP stdio 서버·클라이언트, 직접 의존성 및 환경 lock을 추가했다. Python 3.14.6, MCP SDK 1.30.0, 실제 협상 프로토콜 2025-11-25.

- 신규 6개와 기존 테스트를 합쳐 81개 통과. 승인 전 실행 거부·별도 DB 연결 재실행·티켓/감사 단일성, 다른 사용자/조직, 권한 회수 후 조회/재실행, 변경 초안의 재확인, 엄격한 인자·신원 필드 거부, 감사 실패 시 티켓 롤백을 검사했다.
- 실제 mcp_ticket_demo.py 종료 코드 0. 서버 하위 프로세스 초기화·세 도구 목록·실제 호출·정상 종료를 실행했다. 승인 도구는 목록에 없었다. 승인 전 isError=True, 승인 후 생성/조회, 재실행의 같은 ID 및 replayed=True, 권한 회수 후 오류, tickets 행 1개를 assert로 확인했다. experiments/mcp-tickets.json에 기록했다.
- 첫 실행은 단순 dict 반환의 structuredContent 누락으로 실패했다. SDK 타입 처리 코드를 확인하고 문자열 키 타입과 structured_output=True를 지정해 해결했다. 오류를 성공으로 기록하지 않았다.
- npm run build 종료 코드 0, Pagefind 147페이지. verify_site.py가 11장과 표지의 경로·링크·앵커·canonical·두 사이트맵을 검증했다.

한계: 승인 이벤트는 테스트 픽스처다. 단일 고정 주체의 로컬 stdio만 실행했고 원격 OAuth/HTTP·사용자 UI·모델 도구 선택은 미검증이다. 승인 만료·취소 API와 운영 감사 보관/변조 방지는 없다. 같은 초안의 멱등성을 제공하며 같은 내용으로 만든 다른 초안의 중복은 합치지 않는다. 도구 출력 객체는 구체적 필드별 Pydantic 모델로 아직 제한하지 않았다.

이전 goal 턴은 10장 집필·실측·오류 수정의 progress였으며 이번 턴도 구현·실제 MCP 실행·본문을 추가한 progress다. 전체 목표는 미완료다.


## 2026-09-08: 12장 워크플로·에이전트 제어

본문 8,374자(공백·frontmatter 포함). agent_loop.py와 agent_demo.py를 추가했다. 허용 행동은 조회/초안/종료이며 승인·등록은 제외했다. 입력 중복 JSON 키와 도구별 인자·결과 스키마를 검사한다.

- 총 89개 unittest 통과. 신규 8개는 관찰 후 종료·크레딧 합계, 무행동 상태, 반복 호출·예산, 단계 한도, 계획 후 마감의 실행 차단, 도구 후 마감의 unknown 처리, 잘못된 계획/결과, 계획기의 관찰 변조 방지를 확인했다.
- agent_demo.py 종료 코드 0. 실제 임시 SQLite 티켓 DB와 대역 계획기로 초안 후 확인 대기(티켓 0개), 픽스처 승인 후 등록, 조회/종료(2단계·4가상 크레딧), 반복 감지와 예산 차단을 assert로 확인했다. experiments/agent-control.json에 기록했다.
- npm run build 종료 코드 0, Pagefind 148페이지. verify_site.py는 12장과 표지의 링크·앵커·canonical·두 사이트맵을 확인했다.

한계: 실제 모델 계획 능력, 동적 계획의 품질 개선, 다중 에이전트 비교, SDK 실행은 미검증이다. 시간 검사는 호출 경계의 검사이며 동기 호출의 강제 취소가 아니다. 가상 크레딧은 실제 금액 한도를 보장하지 않는다. 결과의 딕셔너리 반환/스키마 검사는 네트워크 수신 상한이 아니다. 실행 체크포인트·프로세스 재시작 후 자동 복구는 미구현이다.

이전 goal 턴은 11장 집필·업무 구현·실제 MCP 연결의 progress였다. 이번 턴도 제어 구현·검증·본문을 추가한 progress이며 전체 목표는 미완료다.

## 2026-09-08: 13장 파인튜닝 판단과 데이터 분리

- tuning_data.py 실행 종료 코드 0. 표본 9행·계열 5개·train 4/dev 2/test 3행을 확인하고 experiments/tuning-data-check.json에 기록했다.
- 전체 unittest 97개 통과. 신규 8개는 정상 표본, 계열 분할 누수, 식별자·공백 변경 동일 입력 누수, 없는 인용, 필수 필드 누락, 분할 부재, 중복 ID, 의미 오답의 검사 한계를 확인했다.
- npm run build 종료 코드 0. verify_site.py가 13장·표지·내부 링크·앵커·canonical·두 사이트맵을 확인했다. 실제 브라우저·검색 질의 검증은 이번 실행에 포함하지 않았다.
- fine-tuning-decision.md에 학습 보류의 근거와 다음 비교 순서를 기록했다. 가중치 학습과 학습 후 성능 측정은 미실행이다. 표본은 공개된 교육용 데이터이며 학습에 충분하거나 독립 평가에 적합하다고 주장하지 않는다.

전체 목표의 완료가 아닌 13장 본문·검사 예제의 진행 기록이다.

## 2026-09-08: 14–18장·부록 및 최종 검증

- 원고 18장·부록 A–E 5편, 총 23개 본문. 약 17만 9천 자(공백·코드·frontmatter 포함). 빈 장/TODO/본문 작성 예정 표시 없음.
- 전체 unittest 106개 통과. 신규 9개는 공격 경계 3개, 평가 집계 3개, CER/오디오 입력 3개. 모든 Python 예제 AST 파싱도 확인했다.
- media_demo.py 종료 코드 0. Tesseract 5.5.3/Pillow 12.3.0, 고정 한국어 tessdata_fast, Whisper tiny 고정 리비전·CPU float32 4스레드. OCR 약 0.111초/CER 0, 1.749초 합성 질문의 ASR 약 0.217초/CER 0.4545. 실제 오인식 문구를 보존했다. 합성 표본 하나의 결과이며 실제 사용자 일반 성능이 아니다.
- evaluation.py 종료 코드 0. R01–R04 네 건 중 기대 상태 일치 1건, 생성 3건 완료/계약 실패 3건. 의미 평가는 not_scored, 채택 hold. 입력 SHA와 평가기 버전을 기록했다.
- operations_demo.py 종료 코드 0. 각 버전 20개 일괄 요청, 큐 4/워커 2. 정상 ok=4/overloaded=16, 장애 deadline_exceeded=4/overloaded=16, 복귀 ok=4/overloaded=16. 실제 모델 처리량이 아닌 asyncio 합성 대기 실험이다.
- service_smoke.py 종료 코드 0. 실제 E5/Qwen 로컬 가중치·파일 DB·별도 HTTP/CLI 프로세스 사용. 무토큰 401, 신원 필드 추가 400, v2/v1 실제 질문은 모두 contract_failed. 티켓 생성·재실행 같은 ID/replayed=True. 기본 앱 로그에 질문·로컬 토큰이 없음을 assert로 확인했다. 토큰 비교의 바이트 처리 수정 후 최종 재실행도 종료 코드 0이며 service-local-final.json에 별도 보존했다.
- npm run build 종료 코드 0, Pagefind 159페이지. 정적 검사기는 23개 본문+표지, 내부 링크·앵커·canonical·두 사이트맵을 통과했다. 예제 ZIP 링크의 실제 정적 파일도 확인한다.
- Chromium/Playwright로 1440px·390px 각 23개 본문(46조합)의 제목·현재 장·문서 가로 넘침을 검사했다. 모바일 서랍 열기/닫기, 다음 장 이동 후 현재 표시 갱신, Pagefind의 “파인튜닝”·“음성”·“멱등성” 검색에서 이 책의 결과를 확인했다. site-browser.json에 기록. 화면 캡처를 직접 확인했다.
- package_examples.py는 예제·실험의 허용 파일만 ZIP에 묶는다. 모델/환경/비밀/합성 바이너리를 제외한다. 부록 D에 다운로드와 설치·실행 순서를 제공한다.

한계는 FINAL-DESIGN.md와 본문에 통합했다. 인터넷 공개 배포·실제 모델 동시 부하·학습·영상/이미지 생성 모델은 미실행이다. 책의 완료를 예제 모델의 운영 채택으로 표현하지 않는다. 기존 package.json/package-lock.json 및 다른 콘텐츠의 작업을 덮어쓰거나 되돌리지 않았다.
